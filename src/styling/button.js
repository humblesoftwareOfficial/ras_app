import { StyleSheet } from "react-native";

export const BUTTON_STYLE = StyleSheet.create({
  button: {
    margin: 5,
    marginTop: 15,
    padding: 10,
    alignItems: "center",
    borderRadius: 10,
  },
});
